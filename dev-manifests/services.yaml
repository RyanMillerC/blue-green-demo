apiVersion: v1
kind: Service
metadata:
  name: frontend-dev
spec:
  selector:
    app: frontend-dev
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: backend-dev
spec:
  selector:
    app: backend-dev
  ports:
    - port: 8080
      targetPort: 8080
